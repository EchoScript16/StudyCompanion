services:
  - type: web
    name: study-backend
    runtime: python
    envVars:
      - key: GROQ_API_KEY
        sync: false
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
    plan: free
